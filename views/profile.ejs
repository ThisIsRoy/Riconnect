<% include partials/header %>

<div class="container">
    <div class="row">
        <div class="col-md-8 col-offset-2">
            <h1><%= user.firstname + " " + user.lastname %></h1>
            <h3><%= "Affilated with " + user.residence + " College" %></h3>
            <a href="mailto:<%= user.email %>"><%= user.email %></a>
        </div>
    </div>
</div>

<% include partials/footer %>